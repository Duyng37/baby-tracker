# Vite frontend: copy to .env.local and fill directly on your machine, not in chat.
# Configure locally; never put service-role, OAuth secret or database password here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=